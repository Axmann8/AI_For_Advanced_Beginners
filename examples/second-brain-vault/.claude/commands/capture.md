---
description: Capture a quick note into the inbox
argument-hint: [your note]
---
Create a new note in `00-Inbox/` from this text: "$ARGUMENTS"

- Title: a short, descriptive title (max 8 words) as the file name.
- Front matter: `type: note`, `status: inbox`, `created: <today>`, and 1–2 tags.
- Body: the text exactly as written, then a one-line `**Next step:**` suggestion if it's actionable.
Reply with just the new file name.
